/**
  ******************************************************************************
  * @file    inputs.h
  * @brief   Обработка органов управления: рычаги тормоза (с антидребезгом),
  *          концевики селектора АКПП, переключатели и их измерение по TIM1 CH4.
  ******************************************************************************
  */

#ifndef __INPUTS_H
#define __INPUTS_H

#ifdef __cplusplus
extern "C" {
#endif

#include "main.h"
#include <stdint.h>

/* Состояния рычагов тормоза (битовая маска). */
#define BRAKE_NONE   0  /* оба отпущены            */
#define BRAKE_RIGHT  1  /* нажат только правый      */
#define BRAKE_LEFT   2  /* нажат только левый       */
#define BRAKE_BOTH   3  /* нажаты оба               */

/* Текущее положение селектора АКПП: 'N', 'D', 'R'. */
extern unsigned char Selector;

/* Признак активного измерения переключателя (блокирует управление мотором). */
extern volatile int switchactivity;

/**
 * @brief  Прочитать рычаги тормоза и вернуть устойчивое (с антидребезгом)
 *         состояние BRAKE_*.
 *
 *         Нажатие любого рычага учитывается мгновенно; «оба отпущены»
 *         подтверждается только после BRAKE_DEBOUNCE_TIME мс стабильности —
 *         до этого удерживается предыдущее подтверждённое состояние.
 */
uint8_t Inputs_GetBrakeState(void);

/** Если оба концевика селектора не активны — перевести в нейтраль ('N'). */
void Inputs_UpdateSelectorNeutral(void);

/** Завершение измерения переключателя по таймауту TIM1 CH4 (из колбэка OC). */
void Inputs_OnSwitchTimeout(void);

#ifdef __cplusplus
}
#endif

#endif /* __INPUTS_H */
